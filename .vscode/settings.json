// Place your settings in this file to overwrite default and user settings.
{
     "eslint.enable": false,
     "files.exclude": {
         "app/**/.js":true, // cuase it is all TS
         "app/**/*.map":true, // i done care         

         "**/.git": true,
         "**/.svn": true,
         "**/.DS_Store": true

     }
}